model:
    keypoints_num: 17
    init_weights: True
    pretrained_path: ''
    input_size: 
        w: 288
        h: 384
    heatmap_size:
        w: 72
        h: 96
    margin_to_border: 1.15 # >=1
    
    ## use an extra mask module branch or only add a channel to
    mask_channel_num: 1

    only_add_mask_channel: True

    extra_mask_module: false
    extra_feature_flag: false
    
    booster_stacks: 1 # meta-booster nums
    boosting_requirement: 0.5

images_root_dir: '../data/coco/images/'
annotation_root_dir: '../data/coco/annotations/'
person_detection_results_path: '../data/coco/person_detection_results/COCO_val2017_detections_AP_H_56_person.json'

train:
    epoch_begin: 0
    epoch_end: 150
    lr: 0.001
    lr_step_size: 30
    lr_decay_gamma: 0.4
    batchsize: 64
    heatmap_peak_sigma_factor: 3
    aug_scale: 0.2
    aug_rotation: 30 
    aug_flip: false
    augmentation: true

    
    
test:
    batchsize: 32
    bbox_score_threshold: 0.0

    confidence_threshold: 0.2
    oks_nms_threshold: 0.95

    ckpt: 'ckpt/pose_resnet_50_384x288.pth.tar'
